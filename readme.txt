Same as uni-nestore but written in c++ with more features and based on UDP mainly (although file sending is still TCP based).

This was a task on the network programming class at uni. It was one of the few worth doing, as it turned out to be quite challenging.
